apiVersion: v1
kind: Service
metadata:
  name: mern-app-service
  namespace: mern
  labels:
    app: mern-app
spec:
  type: NodePort
  selector:
    app: mern-app
  ports:
  - name: http
    port: 80
    targetPort: 5000
    protocol: TCP
    nodePort: 30000